<!DOCTYPE html>
<html lang="fi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Luukku 20</title>
    <style>
        body { margin: 0; background: linear-gradient(180deg, #001f3f, #003366); color: #fff; font-family: 'Arial', sans-serif; text-align: center; overflow: hidden; position: relative; height: 100vh; display: flex; flex-direction: column; justify-content: center; align-items: center; }
        h1 { font-size: 3em; color: #ffd700; text-shadow: 0 0 15px gold; animation: glow 2s ease-in-out infinite; margin-bottom: 20px; }
        @keyframes glow { 0%, 100% { text-shadow: 0 0 10px gold; } 50% { text-shadow: 0 0 30px gold; } }
        .tervehdys { font-size: 1.4em; margin-bottom: 30px; animation: fadeIn 3s; }
        @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
        iframe { width: 80%; max-width: 800px; height: 450px; border-radius: 15px; box-shadow: 0 0 30px rgba(255, 215, 0, 0.5); margin-bottom: 20px; }
        .snowflake { position: absolute; top: -20px; font-size: 2em; color: white; animation: fall linear infinite; pointer-events: none; }
        @keyframes fall { to { top: 100vh; transform: translateY(100vh) rotate(360deg); } }
    </style>
</head>
<body>
    <h1>Luukku 20</h1>
    <p class="tervehdys">Joulun taikaa</p>
    <iframe src="https://www.youtube.com/embed/yXQViqx6GMY" title="Jingle Bells" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
    <script>
        for (let i = 0; i < 100; i++) {
            let flake = document.createElement('div');
            flake.className = 'snowflake';
            flake.innerHTML = '❄';
            flake.style.left = Math.random() * 100 + 'vw';
            flake.style.animationDuration = (Math.random() * 10 + 5) + 's';
            flake.style.opacity = Math.random() * 0.5 + 0.5;
            flake.style.fontSize = (Math.random() * 1 + 1) + 'em';
            document.body.appendChild(flake);
        }
    </script>
</body>
</html>